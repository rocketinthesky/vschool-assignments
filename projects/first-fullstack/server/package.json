{
  "dependencies": {
    "body-parser": "^1.18.2",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "mongo": "^0.1.0",
    "mongoose": "^4.13.7",
    "morgan": "^1.9.0",
    "nodemon": "^1.13.3"
  },
  "scripts": {
    "heroku-postbuild": "cd ../client && npm install --only=dev && npm install && npm run build"
  },
  "engines": {
    "node": "6.11.4"
  }
}
